import { language } from '~/interfaces/language';

const home = {
  coming_soon: 'Sitio en construcción',
};

const timeline = {};

const es: language = {
  ...home,
  ...timeline,
};

export default es;
