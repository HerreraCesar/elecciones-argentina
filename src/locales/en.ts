import { language } from '~/interfaces/language';

const home = {
  coming_soon: 'Site under construction',
};

const timeline = {};

const en: language = {
  ...home,
  ...timeline,
};

export default en;
